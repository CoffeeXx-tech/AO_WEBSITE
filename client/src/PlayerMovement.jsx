// PlayerMovement.jsx
import { useFrame } from '@react-three/fiber';

export default function PlayerMovement() {
  // aktualnie nic nie robimy, bo ruch obsługuje serwer
  useFrame(() => {});
  return null;
}
